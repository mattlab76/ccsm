#compdef ccsm

_ccsm() {
    local -a commands
    commands=(
        '--search:Search sessions'
        '--stats:Show statistics'
        '--cleanup:Review old sessions'
        '--version:Show version'
        '--help:Show help'
    )

    _describe 'command' commands
}

_ccsm "$@"
